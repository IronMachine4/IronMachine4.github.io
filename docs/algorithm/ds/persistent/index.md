# 可持久化数据结构

可持久化数据结构没有想象中那么高端。“可持久化”的意思就是对于每一次操作，都记录这个数据结构的“副本”，在未来可以访问和操作任意的历史版本。简单来说，把数据结构看作 word 文档，可持久化就是这个文档可以一次操作撤回到你打任何一个字之前。

一个显而易见的可持久化方式就是对于每个版本都暴力复制一份。但这样做无论在时间还是空间上都是爆炸的。所以，该目录接下来的工作，就是把可持久化的时空复杂度降到可以接受的程度。